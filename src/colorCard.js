/* eslint-disable import/no-anonymous-default-export */
export default ({ name, color }) => (
	<div style={{ backgroundColor: color }} className='flex items-center justify-center'>
		{name}
	</div>
);
